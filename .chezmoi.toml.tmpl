{{ $email := promptString "email" -}}
{{ $name := promptString "name" -}}
[git]
  autoCommit = true
  autoPush = true
[edit]
  command = "nvim"
[data]
    email = {{ $email | quote }}
    name = {{ $name | quote }}
